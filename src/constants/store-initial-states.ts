import { TAlgoSliceState } from '../types/store.types';

// eslint-disable-next-line import/prefer-default-export
export const algoInitialState : TAlgoSliceState = {
  isDone: false,
  viewData: [],
  isActive: false,
};
